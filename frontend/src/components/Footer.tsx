export default function Footer() {
  return (
    <footer className="mx-auto max-w-container p-6 text-center text-muted text-[0.82rem] border-t border-gray-200">
      <p>PerformanceLabs &mdash; Prospect Pipeline powered by Claude + Google + Apollo</p>
    </footer>
  );
}
